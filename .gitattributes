# Git 换行与语言统计归一化
# 文本文件统一按仓库内存储格式处理（*.sh 固定 LF，*.bat/*.ps1 固定 CRLF）
* text=auto

# Shell 脚本固定 LF（避免 Windows 检出为 CRLF 导致容器内执行失败）
*.sh text eol=lf

# Windows 脚本固定 CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不进行文本处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary

# 不允许合并的锁文件（保持仓库内版本）
package-lock.json -merge
pnpm-lock.yaml -merge

# GitHub 语言统计修正：
# Vue SFC 归入 Vue 语言；Dockerfile 使用内置语言（无需标注）
*.vue linguist-language=Vue