# 安全政策（Security Policy）

Graw 是一个具备**宿主机 root 级操作能力**的服务器管理面板（详见 README 的安全警告），
安全漏洞可能造成严重后果。请按本政策负责任地披露安全问题。

## 支持的版本

仅支持**最新正式版本**提供安全修复。请始终升级到 `backend/app/main.py` 中 `APP_VERSION`
对应的最新版本（或 Docker 镜像 `shunx/graw:latest` 对应的最新标签版本）。

| 版本状态 | 支持情况 |
|----------|----------|
| 最新正式版 | 提供安全修复 |
| 历史版本 | 请升级到最新版后复测漏洞 |

## 报告漏洞

**请勿**在公开的 Issues、Pull Requests、讨论区或其它公开渠道披露安全漏洞。

请通过私有途径报告：

- 优先：GitHub 私有安全通告（Security Advisory → New draft advisory），选择
  「私有披露 Private vulnerability reporting」让维护者收到通知。
- 备选：在 GitHub Issues 中新建 issue 并在标题前缀 `[SECURITY]`（内容尽量模糊化，
  不包含可利用细节），等待维护者联系你。

### 报告时请包含

1. 受影响版本与部署方式（宿主部署 / Docker 完整宿主机模式）
2. 复现步骤或最小 PoC（如涉及凭据泄露，请先脱敏）
3. 漏洞类型与影响（如：越权、RCE、路径穿越、注入、凭据泄露）
4. 你认为的修复建议（可选）

### 预期响应

- 在 **72 小时**内确认收到报告。
- 修复计划与预计发布时间会在确认漏洞后告知报告者。
- 修复发布后，我们会按惯例在 CHANGELOG 中记录（不披露报告者身份，除非征得同意）。

## 安全设计与部署建议（给用户）

- **仅部署于可信环境**。容器使用 `--privileged` + `--pid host` + 挂载宿主根目录，
  即拥有宿主 root 权限；请勿暴露到公网入口。
- 首次登录后**立即修改默认密码**（默认 `admin / admin123`）。
- 使用强密码并启用双因素认证（2FA）等可用安全设置。
- 保持面板与依赖（FastAPI、paramiko、docker SDK 等）处于最新版本。
- 数据库凭据、SSH 密钥等存于 `backend/data/`，请勿提交到仓库或明文回传前端。

## 通用安全修复政策

- 高风险漏洞（RCE、越权、凭据泄露、认证绕过）优先修复，并会在修复版本里程碑内发布。
- 中低风险漏洞随常规版本滚动修复。
- 我们感谢所有负责任披露的安全研究者的贡献。